import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.docshelf.mobile',
  appName: 'DocShelf',
  webDir: 'dist/frontend-ionic-angular/browser',
};

export default config;
